# Domain Docs

This is a single-context repository.

Before exploring, read `CONTEXT.md` and relevant ADRs under `docs/adr/`
when they exist. Missing domain documents are not blockers.

Use terminology defined in `CONTEXT.md` for tickets, tests, and designs.
Surface any conflict with an existing ADR instead of silently overriding it.
